{
    "enforce_connectivity": "$.dataset.params.enforce_connectivity",
    "inputDir": "$.inputs[0].dataPath",
    "outputDir": "$.dataset.dataPath",
    "platform": "$.dataset.params.platform",
    "sampleID": "$.dataset.params.sample_ID",
    "transcriptsFile": "$.dataset.params.transcripts_file",
    "ignore_z_coord": "$.dataset.params.ignore_z_coord"
}