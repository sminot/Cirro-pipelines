{
  "counts": "$.dataset.params.counts",
  "manifest": "$.dataset.params.manifest",
  "algorithm": "$.dataset.params.algorithm",
  "comp_col": "$.dataset.params.comp_col",
  "comp_ref": "$.dataset.params.comp_ref",
  "group_cols": "$.dataset.params.group_cols",
  "filter": "$.dataset.params.filter",
  "output_folder": "$.dataset.dataPath",
  "web_folder": "$.dataset.s3|/web"
}
